# Add unit tests for the date helpers

The cursor skipped the first item of every page after the first. Adds a regression test that walks three pages.

Change #5 of 5 on branch `pr/20260811-115807-5-add-unit-tests-for-the-date-helpers`.
