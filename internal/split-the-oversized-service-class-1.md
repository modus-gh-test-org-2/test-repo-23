# Split the oversized service class

An empty body used to raise deep inside the parser with an unhelpful stack. It is now handled up front and returns a clear validation error.

Change #1 of 5 on branch `pr/20260811-115807-1-split-the-oversized-service-class`.
